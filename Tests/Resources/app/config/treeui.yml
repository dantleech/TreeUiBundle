twig:
    form:
        resources:
            - 'CmfTreeUiBundle:Form:fields.html.twig'

cmf_tree_ui:
    metadata:
        annotated_document_paths:
            CmfTreeUiBundle: Tests/Resources/Document
    model:
        filesystem:
            root: %kernel.root_dir%
    tree:
        default:
            model: filesystem
            view: static_html
